# StudyBot v4 — Python dependencies
# Install: pip install -r requirements.txt

# Core
streamlit>=1.31.0
anthropic>=0.28.0

# PDF extraction
pdfplumber>=0.10.0
pypdf>=4.0.0

# Voice input (JS bridge for Web Speech API)
streamlit-js-eval>=0.1.7

# Web search for the agent (no API key needed)
duckduckgo-search>=6.0.0
ddgs
